ext {
	jakartaFacesModule = true
}

apply from: "../../build-jakarta-transformer.gradle"

dependencies {
	compileOnly group: "jakarta.faces", name: "jakarta.faces-api", transitive: false, version: "2.3.2"
}